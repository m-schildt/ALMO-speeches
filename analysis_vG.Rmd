---
title: "analysis_v1"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


```{r pressure, echo=FALSE}
library("quanteda")
library("readr")
library("dplyr")
library("readtext")
```

```{r}

mydir = "sample_data"
csvfiles = list.files(path=mydir, pattern="*.csv", full.names=TRUE)
csvfiles

sample_00 = sapply(csvfiles, read_csv)
sample_00



```


```{r}
mydir = "sample_data2"
csvfiles = list.files(path=mydir, pattern="*.csv", full.names=TRUE)
csvfiles

your_data_frame <- do.call(rbind,lapply(csvfiles,read_csv))

sample_corpus <- corpus(your_data_frame, text_field = "Speech")

summary(sample_corpus)

```

Version with readtext
```{r}
your_data_frame2 <- do.call(rbind,lapply(csvfiles,readtext))

sample_corpus2 <- corpus(your_data_frame2, text_field = "Speech")

summary(sample_corpus2)
```